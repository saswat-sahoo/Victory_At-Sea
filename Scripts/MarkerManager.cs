using System.Collections;
using System.Collections.Generic;
using UnityEngine;


public class MarkerManager : MonoBehaviour
{
    public GameObject[] markers;

    public bool[] answered_questions;

    private void Start()
    {
      
    }


    private void Update()
    {
       /* answered_questions[0] = markers[0].GetComponent<question>().solved;
        answered_questions[1] = markers[1].GetComponent<question>().solved;
        answered_questions[2] = markers[2].GetComponent<question>().solved;
        answered_questions[3] = markers[3].GetComponent<question>().solved;
        answered_questions[4] = markers[4].GetComponent<question>().solved;
        answered_questions[5] = markers[5].GetComponent<question>().solved;
        answered_questions[6] = markers[6].GetComponent<question>().solved;
        answered_questions[7] = markers[7].GetComponent<question>().solved;
        answered_questions[8] = markers[8].GetComponent<question>().solved;
        answered_questions[9] = markers[9].GetComponent<question>().solved;

        if (answered_questions[0])
        {
            markers[1].SetActive(true);
        }
        if (answered_questions[1])
        {
            markers[2].SetActive(true);
        }

        if (answered_questions[2])
        {
            markers[3].SetActive(true);
        }

        if (answered_questions[3])
        {
            markers[4].SetActive(true);
        }

        if (answered_questions[4])
        {
            markers[5].SetActive(true);
        }

        if (answered_questions[5])
        {
            markers[6].SetActive(true);
        }

        if (answered_questions[6])
        {
            markers[7].SetActive(true);
        }

        if (answered_questions[7])
        {
            markers[8].SetActive(true);
        }

        if (answered_questions[8])
        {
            markers[9].SetActive(true);
        }

        if (answered_questions[9])
        {
            Debug.Log("You WON");
        }*/


    }


}
